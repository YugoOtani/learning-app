# プロジェクト方針

このリポジトリでは、AIによる開発を前提としつつ、
変更内容・検証結果・リスクを人間が確認できる開発プロセスを採用する。

# アーキテクチャ

- UIはReact + TypeScript。
- Tauri commandは薄く保つ。
- ドメインロジックは `crates/learning-core` に置く。
- UIから永続化層へ直接アクセスしない。
- Tauriに依存しないロジックを優先する。

# 開発方針

- AIによる生成物は `.ai/features/<feature>/` 配下に置く。詳細は `.ai/README.md` を参照する。
- 振る舞いを変更する場合は、実装担当が適切なテストを追加または更新する。
- 実際に実行していない検証を成功したと報告しない。
- 推測と確認済みの事実を区別して報告する。
- 実装・検証・レビューの詳細な手順は対応するSkillに従う。

# 言語

- ユーザーとのやり取りは日本語で行う。
- 仕様書、実装計画、タスク、レビュー結果など、人間が読む開発ドキュメントは日本語で記述する。
